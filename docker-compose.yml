services:
  db:
    image: pgvector/pgvector:pg16
    container_name: prestasearch-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: prestasearch
      POSTGRES_USER: ps
      POSTGRES_PASSWORD: ps_pgvector_2024
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ps -d prestasearch"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - prestasearch

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: prestasearch-backend
    restart: unless-stopped
    expose:
      - "8000"
    env_file:
      - ./backend/.env
    environment:
      - POSTGRES_URI=postgresql://ps:ps_pgvector_2024@db:5432/prestasearch
    dns:
      - 8.8.8.8
      - 1.1.1.1
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 90s
    networks:
      - prestasearch

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: prestasearch-frontend
    restart: unless-stopped
    ports:
      - "3033:80"
    depends_on:
      backend:
        condition: service_started
    networks:
      - prestasearch

networks:
  prestasearch:
    driver: bridge

volumes:
  pg_data:
